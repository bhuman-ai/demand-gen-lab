import BrandRedirect from "../brand-redirect";

export default function Page() {
  return (
    <BrandRedirect
      target="hypotheses"
      title="Hypothesis Queue"
      description="Approve or deny AI-generated sequence ideas inside the active brand."
    />
  );
}
