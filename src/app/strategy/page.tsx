import BrandRedirect from "../brand-redirect";

export default function Page() {
  return (
    <BrandRedirect
      target="campaigns"
      title="Campaigns"
      description="Open campaigns to define objectives, hypotheses, and experiments in the active brand."
    />
  );
}
